# 贡献一个好案例

先读 docs/curation.md 和 docs/data-contract.md。当前只接受原作者的 X 帖子或回复永久链接。

1. 搜索现有 data/cases.json，避免重复收录相同原帖。
2. 用 npm run add 登记候选，或者通过「提交案例」Issue 表单提交线索。
3. 补充原作者、日期、模型声明与证据、原始 Prompt、参考图依赖、成图链接和已取得的授权。没有拿到的内容保持未知。
4. 只有实际可见的互动指标才能填数字，同时记录观察时间。不要跨来源借用热度。
5. 运行 npm run validate、npm test、npm run build，在 PR 中说明收录价值与剩余缺口。

不要直接编辑构建生成的文档；修改 data/ 后重新生成。不要提交 API Key、登录 Cookie、个人参考照片或未经许可复制的第三方图片。

第三方原文、译文、编辑改写和本项目原创模板必须区分。提出删除或归属修正时，请使用「来源纠错或移除」表单，并注明案例 ID 和更正依据。
